import java.lang.*;

public class C {
	public static void main(String[] args) {
		// create scanner object
		Scanner s = new Scanner(System.in);

		// read several types of input
		Integer t = s.nextInt();
		

		/*
    BigInteger A = new BigInteger(a);
    BigInteger B = new BigInteger(b);
    BigInteger C = new BigInteger(c);
    BigInteger D = new BigInteger(d);

    StringBuilder seq = new StringBuilder("");

    while(true){
      int BcompA = B.compareTo(A);
      int DcompC = D.compareTo(C);
      // it's a zero
      if(BcompA >= 0 && DcompC >= 0){
        B = B.subtract(A);
        D = D.subtract(C);
        seq.append("1");
      }
      else if(BcompA <= 0 && DcompC <= 0){ // it's a zero
        A = A.subtract(B);
        C = C.subtract(D);
        seq.append("0");
      }
      else if(BcompA < 0 && DcompC > 0){ // identity matrix
        break;
      }
    }
    System.out.println(seq.reverse().toString());
		*/
	}
}
