#include <iostream>
#include <map>
#include <cstring>
#include <vector>
#include <pair>
using namespace std;

int main () {

	/*
		Solution: 
			- Find out the starting_airport, by maintaining a In and Out count for each airport.
			  The starting airport should have In = 0, Out = 1.
			- Having a map with key = start_airport(left part of ticket), value = end_airport(right part of ticket),
			  start from the determined starting_airport and construct the list by going through the map.
	*/

	return 0;
}